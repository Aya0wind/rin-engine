

/**
 * 播放bgm的实际执行函数
 * @param url bgm的路径
 */
export const playBgm = (url: string) => {
    // // 先停止之前的bgm
    // let VocalControl: HTMLAudioElement = document.getElementById('audio-player');
    // VocalControl.set
    // if (VocalControl !== null) {
    //     VocalControl.currentTime = 0;
    //     if (!VocalControl.paused) VocalControl.pause();
    // }
    // // 播放语音
    // setTimeout(() => {
    //     if (VocalControl !== null) {
    //         VocalControl.currentTime = 0;
    //         VocalControl.oncanplay = () => {
    //             VocalControl.play();
    //         };
    //     }
    // }, 1);
};
